# 《全场一起写代码》方案 C：正式版玩法与系统设计

> 版本：V1.0 评审稿  
> 日期：2026 年 8 月 25 日  
> 方案定义：Godot 本地大屏端 + Web 手机/管理端 + Node.js 权威游戏服务  
> 文档状态：待玩法评审，未进入程序开发  
> 范围说明：本文定义正式参展版，不按 MVP、Smoke Demo 或单场景原型缩减

## 一、评审摘要

《全场一起写代码》应是一款“现场大规模共同编程游戏”，而不是把单人迷宫的按钮复制给几十部手机。正式版需要同时满足：

1. 路人在 5 秒内看懂“扫码后可以给机器人写指令”。
2. 参与者每隔几秒就有一次清晰决策，不需要输入文本代码。
3. 没扫码的观众也能从大屏看懂目标、投票和执行结果。
4. 失败会进入“看轨迹—找错行—换指令—重新运行”的调试环节。
5. 获胜指令由多数决定，但每张有效选票都会贡献协作能量。
6. 每轮可以独立理解，中途加入者不依赖上一轮内容。

建议正式内容范围：

- 3 种常规模式和 1 种定时展演模式；
- 顺序、循环、条件和调试 4 个编程概念；
- 3 章共 15 张常规地图，加 3 张展演关；
- 常规轮次 74—92 秒，进入调试后 92—115 秒；
- 单房间支持 30—100 台手机参与；
- 公网扫码直连为主，本地 Wi-Fi 和自动演示为备用。

## 二、产品定位

### 2.1 一句话定位

全场用手机投票，共同组成一段可视化程序，再在大屏上观看机器人逐行执行、报错和被集体调试。

### 2.2 用户层次

- 路过展位、只停留 1—3 分钟的第一次参与者；
- 不扫码但会观看大屏结果的围观者；
- 愿意连玩多轮、期待循环和条件玩法的参与者；
- 现场主持人和设备管理员。

### 2.3 设计支柱

1. **一眼看懂**：目标、机器人朝向和可投选项无需讲解。
2. **全场有份**：多数票决定指令，每张有效票贡献协作能量。
3. **执行有戏**：程序逐行高亮、逐步演出，不瞬间算完。
4. **错误可读**：碰撞、越界、步数耗尽和逻辑失败有不同反馈。
5. **深度逐层出现**：手机每次仍只做一个简单选择，高级概念由程序脚手架表达。
6. **一轮自洽**：中途加入者可从下一条指令开始参与。

### 2.4 明确不做

- 不要求输入自由文本代码；
- 不设置个人账号、付费道具或个人排行榜；
- 不依赖主持人长时间讲解；
- 不让 Godot 大屏端单独判定结果；
- 不用随机失败或暗中改票制造戏剧性；
- 不开放昵称、聊天或任何会出现在大屏上的自由输入。

## 三、世界观与玩家身份

游戏世界是一座刚刚启动的“新生科技城”。校园服务机器人“小码”的路径程序丢失，需要全场临时组成“人类编译器”，为它重建行动程序。

这个设定只服务于三件事：

- 把投票解释为“提交指令”；
- 把开始执行解释为“编译完成”；
- 把失败解释为“检测到 Bug，进入调试”，不用“你们选错了”惩罚玩家。

剧情文字不超过一句，任务始终用地图、目标图标和动画表达。

## 四、完整单轮流程

### 4.1 状态机

    吸引/等待
      → 加入倒计时
      → 任务预览
      → 编程（投票 ↔ 结果写入）
      → 编译展示
      → 结果预测
      → 逐行执行
          → 成功
          → 或选择可疑行 → 提交补丁 → 重新执行
      → 结算
      → 切换下一关

### 4.2 时间预算

| 阶段 | 时长 | 玩家动作 | 大屏重点 |
|---|---:|---|---|
| 加入倒计时 | 10 秒 | 扫码或继续上一轮 | 二维码、人数、一句话规则 |
| 任务预览 | 6 秒 | 看目标和新机关 | 镜头飞过地图并点亮起终点 |
| 编程 | 30—42 秒 | 每个空位选 1 张指令 | 程序逐行生成、协作能量增长 |
| 编译 | 4 秒 | 无 | 程序折叠成可执行结构，音效蓄力 |
| 预测 | 3 秒 | 可选预测运行结果 | 暂不公布分布 |
| 执行 | 10—16 秒 | 看大屏 | 代码高亮、机器人动画、轨迹 |
| 调试 | 18—23 秒 | 选错行，再选替换指令 | 轨迹回放、错误类型、补丁效果 |
| 结算 | 8 秒 | 查看结果 | 三星成果、知识点、全日进度 |
| 重置 | 3 秒 | 无 | 场景无缝过渡 |

无调试的常规轮次为 74—92 秒；进入调试后为 92—115 秒。等待阶段持续允许扫码，新加入者无需等到下一整轮。

## 五、地图与角色规则

### 5.1 地图规格

- 常规地图最大 10 列 × 8 行，保证大屏网格清晰；
- 每关只有 1 个起点和 1 个主目标，可有 0—3 个次要目标；
- 机器人朝向同时使用箭头、面部朝向和地面光锥提示；
- 核心路径不能只用颜色区分，还要使用图形和动画；
- 每关最多引入 1 种新机关，展演关最多组合 3 种已学机关。

### 5.2 基础指令

| 指令 | 语义 | 程序成本 | 演出时间 |
|---|---|---:|---:|
| 前进 | 沿当前朝向移动 1 格 | 1 步 | 0.55 秒 |
| 左转 | 原地逆时针旋转 90° | 1 步 | 0.4 秒 |
| 右转 | 原地顺时针旋转 90° | 1 步 | 0.4 秒 |

机关互动默认自动发生，不另设“拾取”或“使用”按钮，保持三指令的基础心智模型。

### 5.3 地图元素

| 元素 | 效果 | 反馈 |
|---|---|---|
| 普通地面 | 可通行 | 无 |
| 墙体 | 阻止前进 | 碰撞停机并高亮当前行 |
| 边界 | 不可越过 | 触发“越界异常” |
| 能量芯片 | 踏入后自动收集 | 用于额外挑战星 |
| 压力开关 | 踏入后改变门状态 | 连线动画点亮对应门 |
| 逻辑门 | 根据开关状态开闭 | 关闭时等同墙体 |
| 传送带 | 踏入后自动移动 1 格 | 作为环境事件写入轨迹 |
| 方向地砖 | 只允许指定方向通过 | 违反时停机 |
| 终点 | 踏入后正常终止程序 | 立即成功，不执行剩余行 |

启动版不加入随机敌人、实时操作或隐藏信息，所有结果必须能从地图和程序确定推导。

## 六、编程系统

### 6.1 可视化程序脚手架

玩家不直接编辑自由文本，而是填充关卡提供的“程序脚手架”。低难度是空白顺序表；高难度会预置循环或条件结构，全场只对其中的空位做决定。

这样手机每次仍只显示 2—4 个大按钮，却能在大屏准确展示顺序、循环和条件概念。

### 6.2 顺序程序

    01 [全场选择]
    02 [全场选择]
    03 [全场选择]
    04 [全场选择]
    05 [全场选择]

空位依次开放，每个空位可选“前进、左转、右转”。

### 6.3 循环程序

循环关不让玩家手动摆放括号。地图规定循环体长度，玩家填充循环体，再投票决定重复次数：

    01 重复 [2 / 3 / 4] 次：
    02   [全场选择]
    03   [全场选择]
    04 [全场选择]

大屏保留折叠代码，执行时显示“第 2/3 次循环”，不把循环偷偷展开成普通顺序指令。

### 6.4 条件程序

每张条件关只引入一个可视化判断，例如“前方是否有墙”。谓词由关卡固定，全场分别填充真假分支：

    01 如果 前方有墙：
    02   [左转 / 右转]
    03 否则：
    04   [前进 / 左转 / 右转]

每次判断时，大屏显示“条件 = 真/假”并点亮实际走入的分支。

### 6.5 程序预算

- 常规关由玩家填充 5—7 个空位；
- 展开后的最大执行步数为 16；
- 不允许自由无限循环；
- 超过步数上限时在编译阶段报错，不让大屏卡死；
- 每张地图必须预存至少 1 个标准解和全部已测试可接受解。

## 七、全场投票机制

### 7.1 有效参与者

- 每个浏览器会话获得签名匿名凭证；
- 投票开始时已连接，或在截止前仍剩至少 2 秒时加入的会话，可以参与当前投票；
- 大屏、管理端和自动演示代理不计入玩家人数；
- 断线后保留 10 秒重连宽限，避免人数反复跳动。

### 7.2 投票行为

- 每个参与者每个程序空位最终只有 1 张票；
- 截止前最后 1 秒以外可以改选，以最后一次为准；
- 最后 1 秒锁定按钮，避免边界时刻争议；
- 每次提交必须收到服务端确认，未确认时自动重试；
- 服务端以“轮次 + 空位 + 会话”做幂等校验，重发不会变成重复票。

### 7.3 票数展示

为兼顾现场热闹感和独立思考，票数分三段呈现：

1. 前 60% 时间只显示总参与脉冲，不显示各选项分布；
2. 后 40% 显示三条实时动画柱，但不显示精确人数；
3. 锁票后显示精确票数和获胜指令，持续 0.8—1.2 秒。

彩排时对该规则做 A/B 观察。若现场缺乏气氛，可切成全程显示趋势，但不改变计票规则。

### 7.4 平票、无票和低人数

- 平票时在并列项中做带种子的随机裁决，并明确显示“平票，启动编译器裁决”；
- 种子由轮次号和空位号生成，可回放且不可被管理员暗改；
- 零票时使用地图预设的导航建议，标记为自动驾驶；
- 连接人数低于 3 时仍计算真实选票，其余空位由自动驾驶填充，不伪造玩家人数。

### 7.5 协作能量

- 某个空位的有效投票率达到 70%，点亮 1 格协作能量；
- 能量条最多 3 格，不因投票答案错误而扣除；
- 每轮无条件拥有 1 次标准调试机会；
- 能量全满时解锁“紧急补丁”：第一次调试仍失败后，再获得一次 10 秒补丁机会；
- 不以“大家是否选得一样”奖励能量，避免鼓励盲从。

## 八、编译、执行与预测

### 8.1 编译

服务端把可视化程序转换为带源代码行号的执行轨迹，并检查：

- 所有必填空位已经锁定；
- 循环次数和结构合法；
- 展开后步数没有超限；
- 每个执行事件都能回溯到可编辑源代码行。

程序结构不合法才叫“编译失败”；路线选择错误属于运行或逻辑错误。

### 8.2 执行

- Node.js 服务是唯一规则权威，提前生成带时序的执行轨迹；
- Godot 只按轨迹播放镜头、动画、音效和代码高亮；
- 手机同步显示简化程序和当前行，执行期间不再提供操作；
- 踏入终点后程序正常终止，剩余行标为“未执行”；
- 碰墙、越界或违反方向砖时立即停机并保留轨迹。

### 8.3 预测互动

编译完成后，手机可在 3 秒内预测：

- 一次运行成功；
- 会碰撞或越界；
- 能运行，但到不了终点。

预测不影响程序和分数，只在执行后公布“有多少同学猜对了”。它用于促使玩家在运行前再读一遍程序。

## 九、调试玩法

### 9.1 错误类型

| 错误 | 触发条件 | 反馈 |
|---|---|---|
| 碰撞错误 | 前进目标为墙或关闭门 | 碰撞波纹、停机、红色轨迹点 |
| 越界错误 | 移动目标超出地图 | 边界警报和方向箭头 |
| 机关错误 | 门未开或方向砖不允许 | 门/地砖与关联开关一起闪烁 |
| 逻辑错误 | 程序执行完但未到终点 | 显示实际终点和目标距离 |
| 步数超限 | 循环展开超过上限 | 编译阶段拒绝执行并指向循环行 |

### 9.2 两步调试

失败后不重写整段程序，而是：

1. **选择可疑行**：服务端根据轨迹给出 2—4 个可编辑候选行，全场投票选择要检查的行。
2. **提交补丁**：对选中行展示所有合法替换指令，再次投票。

候选行算法只缩小范围，不直接透露正确答案。碰撞错误的候选集包括报错行和最近影响朝向的几行；逻辑错误则参考实际轨迹与全部已登记可接受解首次发生状态差异的位置。

### 9.3 重新执行

- 修改后从第 1 行重新执行；
- 到达被修改行前可用 2 倍速回放，修改行恢复正常速度；
- 修改前后使用差异块和图标双重展示；
- 首次补丁仍失败时，只有协作能量全满才开放紧急补丁；
- 最终仍失败时，结算后用 6 秒快速展示一个标准解。

## 十、游戏模式

### 10.1 全场共编

默认轮播模式。全场从空白或半结构化程序开始，填充 5—7 个空位，完成编译、执行和可能的调试。它覆盖顺序、循环和条件三个难度层。

### 10.2 Bug 急诊室

系统提供一段必然失败的短程序，先快速执行一次，再让全场通过“选错行 + 选补丁”修复。

- 时长 55—75 秒；
- 候选 3—5 行；
- 常规轮只有 1 个关键错误；
- 用于高峰快速翻台，或连续两轮共编后切换节奏。

### 10.3 逻辑实验室

专门使用循环、条件和机关的进阶模式。它不增加手机单次操作难度，而通过程序脚手架提高全局思考难度。

- 时长 90—120 秒；
- 一关最多同时使用 1 个循环和 1 个条件；
- 新机关先用 3 秒动画示范；
- 默认只在至少 8 名参与者时自动轮入。

### 10.4 整点核心战

每 20—30 分钟由主持人启动一次 150—180 秒展演关。关卡包含 3 个检查点，每段只编译一个短程序块：

1. 顺序路径；
2. 开关与逻辑门；
3. 循环或条件结构。

中途加入者可从下一个空位开始参与；通关触发完整舞台动画和当日总成果。该模式不进入普通随机轮播，避免路人一加入就落入过长轮次。

## 十一、关卡和难度体系

### 11.1 正式内容量

| 章节 | 关卡数 | 核心概念 | 主要机关 | 首次执行目标成功率 |
|---|---:|---|---|---:|
| 第 1 章：启动区 | 5 | 方向、顺序、终止 | 墙、能量芯片 | 65%—80% |
| 第 2 章：机关工厂 | 5 | 状态变化、因果 | 开关、门、传送带 | 50%—70% |
| 第 3 章：逻辑网络 | 5 | 循环、条件 | 方向砖、条件路径 | 40%—60% |
| 展演关 | 3 | 组合应用 | 已学机关组合 | 最终通关 70%—85% |

目标成功率用于调优地图，不允许系统暗中改变投票或执行结果。所有结果必须由地图、程序和公开规则决定。

### 11.2 自动关卡导演

系统根据最近 5 轮匿名聚合结果选择下一张地图：

- 连续 2 轮首次通关：下一轮提高 1 级难度；
- 连续 2 轮调试后仍失败：下一轮切回启动区或 Bug 急诊室；
- 参与人数低于 8：不自动选择逻辑实验室；
- 新机关在本次活动第一次出现时，先使用它的教学关；
- 同一地图在最近 4 轮内不重复；
- 管理端可以关闭自动导演并固定章节、模式或关卡。

### 11.3 关卡作者规范

每张地图必须附带：

- 唯一 ID、名称、章节、难度和主题；
- 网格、起点、朝向、终点和机关参数；
- 程序脚手架及每个空位的合法选项；
- 标准解、已知可接受解和展开步数；
- 每种失败的候选调试行生成规则；
- 零票导航建议和自动演示解；
- 预览镜头、机关提示和结算知识点；
- 至少 20 组自动程序检查用例。

### 11.4 单关设计红线

- 不能依靠主持人口头说出机器人初始朝向；
- 不能出现视觉上相同、规则上不同的地砖；
- 不能把必经路线藏在二维码或程序面板后方；
- 不能用“试过才知道”的随机机关；
- 不能让唯一正确解依赖最后一秒抢票；
- 不能让标准解执行超过 16 个原子步骤；
- 不能以失败率高作为“有挑战”的唯一证明。

## 十二、胜利、评价与长线反馈

### 12.1 胜负规则

- 机器人踏入终点：本轮成功；
- 调试或紧急补丁后到达终点：成功并标记“调试通过”；
- 用完补丁仍未到达终点：本轮未通过，随后展示标准解；
- 文案不使用“全场失败”“太菜”等惩罚性表达。

### 12.2 全场三星

不做个人排名，每轮只评价全场的三个不同维度：

1. **任务星**：最终到达终点；
2. **算法星**：展开步数不高于关卡标准步数；
3. **协作星**：大多数投票窗口的参与率达到 70%。

另用不占星级的徽章表示“一次过”“Bug 猎人”“全员提交”和“最短程序”。调试不扣任务星，也不被描述成低水平表现。

### 12.3 全日共同成果

等待页展示当日聚合数据：

- 参与人次；
- 全场提交的指令总数；
- 修复的 Bug 数量；
- 成功送达次数；
- “科技城供电进度”。

进度达到阶段目标时解锁城市亮灯演出，但不锁住真实关卡，避免早到和晚到的人获得不同玩法权限。

## 十三、手机参与体验

### 13.1 扫码后的完整流程

    扫码打开网页
      → 自动进入当前房间
      → 首次显示 3 秒图示教学
      → 当前可投票时立即显示指令卡
      → 提交并收到服务端确认
      → 等待下一条指令
      → 执行时同步查看当前代码行
      → 参与调试或查看结算
      → 默认留在下一轮

### 13.2 手机页面状态

| 状态 | 主要内容 | 操作 |
|---|---|---|
| 连接中 | 机器人连接动画 | 无 |
| 已加入 | 当前人数和轮次阶段 | 无 |
| 首次教学 | “看大屏地图，在这里选下一条指令” | 我知道了 |
| 投票 | 问题、时间、2—4 张指令卡 | 选择或改选 |
| 已提交 | 选中指令、确认状态、“请看大屏” | 截止前可改选 |
| 执行 | 简化代码、当前行、预测结果 | 无 |
| 调试 | 候选代码行或补丁卡 | 投票 |
| 结算 | 全场三星、徽章、下轮倒计时 | 继续参与或离开 |
| 断线 | 自动重连进度 | 手动重试 |

### 13.3 中途加入

- 截止前剩余至少 2 秒：可参与当前投票；
- 剩余不足 2 秒：显示当前结果，等待下一条指令；
- 执行或结算中加入：显示简化轨迹和“下一次投票马上开始”；
- 超过安全容量时才进入只看模式，不用“房间已满”直接拒绝访问。

### 13.4 参与者反馈原则

- 手机上的选择一经服务端确认，要有清晰的颜色、图标和可选短振动；
- 不能只显示“提交成功”，还要重复显示玩家选择了哪条指令；
- 若投票未计入，要明确说明是“已截止”还是“连接失败”；
- 手机不持续播放与大屏重复的复杂动画，鼓励玩家抬头看公共结果；
- 结算页不强迫跳转社团报名，报名入口放在次要位置。

## 十四、大屏信息与演出

### 14.1 信息层级

1. 地图、机器人和目标是第一视觉层；
2. 当前问题和倒计时是第二层；
3. 正在形成的程序是第三层；
4. 二维码、人数和协作能量常驻但不抢主视觉；
5. 统计和社团信息只在等待或结算阶段展开。

### 14.2 远距离可读性

- 核心文字字高不小于实际屏高的 3.2%；
- 倒计时同时使用数字和环形缩减；
- 指令始终使用“箭头图标 + 文字 + 固定位置”；
- 成功、碰撞和等待同时使用形状、动画和声音，不只用颜色；
- 二维码周围保留静区，特效不从二维码上方掠过；
- 地图和程序面板在 5 米观察距离完成真实验收。

### 14.3 声音规则

- 锁票、编译、单步执行、碰撞、修复和通关拥有独立音效；
- 关闭声音也能完整理解结果；
- 倒计时最后 3 秒才明显蓄力，避免整轮持续制造压力；
- 管理端可分别调整主音量、音乐和效果音。

## 十五、管理与主持体验

### 15.1 自动运营

默认做到“没有主持人也能连续运行”：

- 自动选关和切换模式；
- 自动开放、锁定和结算投票；
- 零票时自动使用导航指令；
- 低人数时进入半演示状态；
- 轮次结束后清理临时票据。

### 15.2 管理权限

- 开始、暂停、继续、跳过阶段、紧急重置；
- 选择章节、模式、关卡和投票时长；
- 锁定自动导演或开启整点核心战；
- 查看手机、大屏、公网入口、本地网络和服务健康状态；
- 切换公网二维码、本地备用二维码或只演示模式；
- 调整声音和票柱显示方式；
- 导出不含个人标识的活动统计。

管理员不能修改已锁票数或暗中指定获胜指令。紧急跳过会在大屏标注“现场控制”，并记入日志。

## 十六、现场网络与异常规则

### 16.1 正常连接方式

- 手机通过公网域名访问现场电脑里的游戏服务；
- 大屏和管理端访问本机地址，不绕行公网；
- 公网只开放参与页面和玩家通信端点；
- 现场另备一台关闭客户端隔离的独立路由器。

### 16.2 异常降级

| 异常 | 系统处理 | 可见反馈 |
|---|---|---|
| 单部手机断线 | 保留会话 10 秒，重连后发送完整快照 | 正在恢复本轮 |
| 投票提交超时 | 带幂等键重试，截止后拒绝迟到票 | 提交中或本次未计入 |
| 公网隧道断开 | 大屏和管理端继续本地运行 | 显示备用 Wi-Fi 指引 |
| Godot 大屏断开 | 服务在当前安全边界暂停计时 | 手机显示大屏正在恢复 |
| Godot 重连 | 下发快照和未完成轨迹 | 从安全点恢复，不重开整轮 |
| Node.js 重启 | 从最近检查点恢复，未锁投票重新完整计时 | 本步重新投票 |
| 无参与者 | 使用预存解自动演示 | 二维码持续开放 |
| 服务无法恢复 | Godot 切换内置演示列表 | 隐藏无效二维码，不黑屏 |

### 16.3 安全暂停边界

只允许在这些时刻暂停或恢复：

- 新投票尚未开始；
- 某空位已锁票并写入程序；
- 一个原子执行动作已经播放完成；
- 调试的选行或补丁投票已经锁定。

这避免同一条指令计票两次，或动画只播放半步。

## 十七、方案 C 的技术职责

### 17.1 本地运行拓扑

    现场笔记本
    ├── Node.js 24 LTS 游戏服务 :3000
    │   ├── Fastify HTTP：手机页面、管理 API、健康检查
    │   ├── WebSocket：玩家、大屏、管理实时协议
    │   ├── 权威状态机、投票、编译器和执行器
    │   └── SQLite：检查点、配置索引、匿名聚合统计
    ├── Godot 4.x Linux 大屏程序
    │   └── 地图、角色、代码高亮、特效和音频
    ├── React + Vite Web 应用
    │   ├── /join 手机参与端
    │   └── /admin 本地管理端
    ├── 正式公网隧道：只转发手机参与流量
    └── 独立路由器：断网时本地备用

### 17.2 服务端职责

- 房间、轮次和权威计时；
- 匿名会话、重连和每步一票；
- 票数锁定、平票裁决和协作能量；
- 顺序、循环、条件、碰撞和机关规则；
- 成功、失败、调试候选和补丁合法性；
- 生成可回放的带行号执行轨迹；
- 阶段检查点、崩溃恢复和聚合统计。

任何客户端都不能自行宣布投票获胜或关卡通过。

### 17.3 Godot 大屏职责

Godot 负责演出，不负责裁判：

- 从服务端快照构建可见场景；
- 按轨迹序号播放移动、转向、机关、镜头和音效；
- 播放完成后确认已经展示的最大轨迹序号；
- 重连后跳到最近安全边界，不重复已确认事件；
- 所有素材本地缓存，现场不从公网下载；
- 服务完全失效时可播放有限的内置演示列表。

### 17.4 Web 端职责

- 手机端采用竖屏布局，主操作无需滚动；
- 所有指令卡同时提供图标和文字；
- 收到完整快照即可重建页面，不依赖此前所有增量消息；
- 管理端和玩家端使用不同身份与协议权限；
- 紧急重置等破坏性命令必须二次确认。

### 17.5 实时协议

每条消息至少包含：协议版本、消息类型、房间、轮次、序号、服务端时间戳和负载。

主要消息族：

- 客户端到服务端：加入、投票、选择调试行、选择补丁、大屏确认、管理命令；
- 服务端到客户端：完整快照、阶段变化、票数趋势、程序更新、执行轨迹、轮次结果、系统健康。

协议不暴露 Godot 场景节点名或 React 组件状态，避免两个界面实现彼此绑定。

## 十八、数据、隐私与防滥用

### 18.1 保存的数据

- 关卡、模式、轮次时间和聚合参与人数；
- 各选项聚合票数，不长期保存某台设备的具体选择；
- 最终程序、执行结果、调试行和补丁；
- 阶段用时、断线、重连和服务健康事件；
- 当日统计及关卡导演所需的最近结果。

### 18.2 不保存的数据

- 姓名、手机号、学号、微信、人脸或精确位置；
- 跨活动追踪标识；
- 个人胜率、排名或行为画像；
- 除短期安全限流外的长期 IP 记录。

### 18.3 防滥用

- 签名匿名会话、每空位一票、服务端幂等；
- 会话发放、重连、投票和管理命令分别限流；
- 不提供自由文本和文件上传；
- 不做侵入性浏览器指纹，接受娱乐展示场景下少量无法彻底防止的多开；
- 管理端只允许本地访问或使用独立高强度密钥。

## 十九、可访问性与包容性

- 手机主按钮触摸区不小于 48 × 48 CSS 像素；
- 所有操作可以单手完成，不使用拖拽、双指或精确按压；
- 颜色不是唯一信息载体，保留图标、文字和图案；
- 倒计时结束前提供可见变化和可选短振动，不依赖声音；
- 不使用高频全屏闪烁；
- 手机端遵循减少动态效果偏好；
- 中文为主，指令可附小号英文 MOVE、TURN LEFT、TURN RIGHT；
- 新玩家不因没看过前置教学而失去操作资格。

## 二十、验收指标

### 20.1 玩法验收

| 维度 | 正式验收标准 |
|---|---|
| 可理解性 | 至少 80% 首次参与者无口头指导时能在 8 秒内完成第一票 |
| 等待感 | 加入到第一次可操作的中位时间不超过 8 秒 |
| 节奏 | 90% 无调试常规轮在 92 秒内结束 |
| 参与度 | 平均每个投票窗口的有效投票率不低于 65% |
| 难度 | 三章首次成功率分别稳定在目标区间 |
| 调试价值 | 首次失败轮中至少 75% 能在标准调试后通关 |
| 最终体验 | 常规关调试后的总通关率为 80%—90% |
| 知识理解 | 至少 70% 受访者能说出“程序按顺序执行” |
| 公平性 | 可由地图、票数、平票种子和规则重现任意结果 |
| 现场容量 | 30 台真机无状态分叉，100 连接模拟压测下锁票正确 |

### 20.2 单关验收

每张关卡必须通过：

- 标准解和全部已知解的自动执行测试；
- 合法指令组合的有界穷举或等价性质测试；
- 碰撞、越界、未到终点、步数超限和机关状态测试；
- 至少 80% 的失败轨迹能生成包含可修复行的候选集；
- 5 米观看、色觉缺失模拟和无声模式测试；
- 至少 2 轮由未接触项目的学生参加的盲测。

## 二十一、完整制作范围与排期

### 21.1 正式版必须交付

- 15 张常规关和 3 张展演关；
- 全场共编、Bug 急诊室、逻辑实验室、整点核心战；
- 顺序、循环、条件和两步调试；
- Godot 大屏、手机参与端、管理端和权威服务端；
- 公网直连、本地 Wi-Fi 备用和无服务演示三级降级；
- 正式视觉、动画、声音、无声可读和减少动态效果适配；
- 聚合统计、健康监控、重连、检查点恢复和导出；
- 运行手册、网络切换卡、管理快捷表和故障演练脚本。

### 21.2 建议人员

- 1 人负责服务端、规则引擎、实时协议和部署；
- 1 人负责 Godot 大屏、动画、声音和表现；
- 1 人负责手机/管理 Web、可访问性和响应式适配；
- 1 人可兼职负责关卡、文案、玩法测试和现场彩排。

若只有 2 名开发，建议服务端成员兼 Web，Godot 成员兼关卡，但保留独立盲测人员。

### 21.3 建议排期

| 周次 | 完成目标 |
|---|---|
| 第 1 周 | 冻结规则、关卡格式、协议和视觉方向 |
| 第 2 周 | 状态机、规则执行器、投票和自动测试 |
| 第 3 周 | 手机主流程、管理端、Godot 场景和轨迹播放基础 |
| 第 4 周 | 共编与 Bug 急诊室全流程、重连和检查点 |
| 第 5 周 | 逻辑实验室、核心战、自动导演和全部关卡灰盒 |
| 第 6 周 | 关卡调优、正式视觉、动画、音效和手机兼容 |
| 第 7 周 | 网络切换、100 连接压测、崩溃恢复和数据验证 |
| 第 8 周 | 20—30 台真机彩排、盲测、调难度、故障演练和封版 |

按 3 名主力加 1 名兼职内容/测试估算为 8 周；只有 2 名开发时建议 10—12 周，不通过删除异常处理压缩日期。

## 二十二、主要风险与玩法对策

| 风险 | 后果 | 对策 |
|---|---|---|
| 玩家只跟随多数票 | 独立思考消失 | 前 60% 隐藏选项分布，后段才显示趋势 |
| 少数派觉得没用 | 不再继续投票 | 每张有效票贡献协作能量 |
| 全场连续选错 | 体验沮丧 | 标准调试、满能量紧急补丁、最终标准解 |
| 高级概念难懂 | 手机操作停顿 | 循环和条件由脚手架预置，每次只填一个空 |
| 执行动画太慢 | 玩家低头离开 | 原子步骤约 0.4—0.55 秒，调试回放可 2 倍速 |
| 关卡只有唯一思路 | 讨论价值低 | 中后期关保留两种可读解，以算法星鼓励最短解 |
| 关卡太容易 | 调试不出现 | 自动导演提难度，Bug 急诊室确保调试轮播 |
| 现场太吵 | 讲解失效 | 核心信息不依赖声音，图示和短句独立教学 |
| 中途加入者多 | 不懂前文 | 始终下发完整快照，每个问题自带上下文 |
| 展演关过长 | 路人被占用 | 只定时或手动开启，不进入普通轮播 |

## 二十三、评审需要拍板的问题

本轮建议只评审以下玩法和范围决策，视觉风格在玩法冻结后单独评审：

1. 是否以“顺序 + 循环 + 条件 + 两步调试”作为正式版知识边界；
2. 是否同意高级程序使用预置脚手架，不在手机自由拖拽程序块；
3. 是否采用“前 60% 隐藏分布、后 40% 显示趋势”的投票反馈；
4. 是否每轮固定有 1 次调试，高参与度才解锁紧急补丁；
5. 是否接受 15 张常规关加 3 张展演关；
6. 是否接受常规轮 74—92 秒、调试轮上限约 115 秒；
7. 是否不做个人账号和排行榜，只保留全场三星和全日成果；
8. 是否让整点核心战只由定时或管理员开启；
9. 是否采用“服务端裁判，Godot 只演出”的职责分离；
10. 是否采用“公网直连为主 + 本地 Wi-Fi 备用 + 无服务演示保底”。

## 二十四、建议评审结论

建议原则上通过方案 C 及上述主循环，但在开发前冻结四个会影响全部内容制作的参数：

- 投票展示方式；
- 标准调试和紧急补丁次数；
- 正式关卡数量；
- 单轮目标时长。

这四项冻结后再大规模制作关卡、投票界面和执行演出，可以避免玩法评审后出现结构性返工。
